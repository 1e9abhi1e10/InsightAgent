# --- LLM (required) ---
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-flash-lite-latest

# --- Local dev only ---
# Point the Next.js frontend at the local FastAPI backend.
# Set this when running `npm run dev` (not needed on Vercel).
BACKEND_ORIGIN=http://localhost:8000
